import { FC } from 'react';
import Logo from '../Logo';

const Navbar: FC = () => {
  return (
    <nav className="flex ">
      {/* Logo */}
      <Logo />

      {/* Navbar */}

      {/* Profile or Login with Theme Swithcer */}
    </nav>
  );
};

export default Navbar;
